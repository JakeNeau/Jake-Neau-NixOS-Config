This machine and my Claude Code config are managed declaratively by Nix.

Before creating or editing any skill, agent, slash command, rule, hook, or MCP
server — or changing global Claude config — use the **claude-code-config** skill.
For how this machine is laid out (and where the Nix config lives), use the
**machine-layout** skill.
