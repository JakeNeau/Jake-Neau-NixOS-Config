# Synthesize refinement evidence

Combine the supplied evidence artifacts without adding unsupported claims.
Resolve duplicate findings and state any conflict between sources. Preserve the
source paths in the input artifacts through concise factual references.

Produce the facts, constraints, conflicts, integration points, and material
open questions needed for user decisions. Remove questions that project
evidence already answers.

Return one `evidence-synthesis` artifact with `workflow_output`.
