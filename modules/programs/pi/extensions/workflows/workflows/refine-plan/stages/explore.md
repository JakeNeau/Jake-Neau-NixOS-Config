# Explore implementation requirements

Let the user lead a conversation about the intended implementation. Respond
naturally with relevant mechanisms, alternatives, trade-offs, and reflections.
Do not turn the conversation into a sequence of clarification questions. Stay
in exploration mode until the user explicitly requests another stage.

When the parent requests finalization, return one `conversation-summary`
artifact with `workflow_output`.
